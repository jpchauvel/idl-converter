# IDL 0.30.x to Classic Version Converter

This program converts IDL 0.30.x files to Classic Version files (in-place).

## Build

```sh
cargo build --relase
```

## Usage

```sh
./target/release/idl-converter path/to/file.idl
```
